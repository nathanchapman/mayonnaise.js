const characters = require('./characters');
const places = require('./places');
const patrick = require('./quotes');
const timecards = require('./timecards');

module.exports = {
  characters,
  places,
  patrick,
  timecards,
};
